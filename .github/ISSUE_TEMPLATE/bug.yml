name: 🐛 Báo lỗi
description: Báo cáo lỗi phần mềm
labels: ["bug"]
body:
  - type: dropdown
    id: bug-type
    attributes:
      label: Loại lỗi
      options:
        - Nhảy chữ / chữ bị đảo vị trí
        - Dính chữ / chữ bị gộp lại
        - Mất dấu / không lên dấu
        - Sai dấu / dấu đặt sai vị trí
        - Gõ ra kết quả không như mong đợi
        - Gõ tiếng Việt không hoạt động
        - Ứng dụng bị crash / đóng đột ngột
        - Không bật được Gõ Nhanh
        - Khác
    validations:
      required: true

  - type: dropdown
    id: app-scope
    attributes:
      label: Lỗi xảy ra ở đâu?
      description: Lỗi chỉ xảy ra trên một app cố định hay tất cả các app?
      options:
        - Chỉ một app cố định
        - Một vài app nhất định
        - Tất cả các app
        - Không chắc chắn
    validations:
      required: true

  - type: input
    id: affected-apps
    attributes:
      label: App bị lỗi (nếu biết)
      description: Tên app bị lỗi, ví dụ Chrome, VS Code, Telegram...
      placeholder: "Chrome, VS Code..."

  - type: textarea
    id: description
    attributes:
      label: Mô tả lỗi
      placeholder: Lỗi gì đã xảy ra? Gõ gì thì bị sai?
    validations:
      required: true

  - type: textarea
    id: reproduce
    attributes:
      label: Các bước tái hiện
      placeholder: |
        1. Mở app X
        2. Gõ "abc"
        3. Kết quả bị sai thành "xyz"

  - type: dropdown
    id: input-method
    attributes:
      label: Kiểu gõ đang dùng
      options:
        - Telex
        - VNI
        - Không biết
    validations:
      required: true

  - type: input
    id: typed-input
    attributes:
      label: Bạn đã gõ gì?
      description: Các phím bạn đã bấm (ví dụ "vieetj")
      placeholder: "vieetj"

  - type: input
    id: expected-output
    attributes:
      label: Kết quả mong đợi
      description: Từ bạn muốn nhận được
      placeholder: "việt"

  - type: input
    id: actual-output
    attributes:
      label: Kết quả thực tế
      description: Từ bạn nhận được (bị sai)
      placeholder: "viêtj"

  - type: dropdown
    id: os
    attributes:
      label: Hệ điều hành
      options:
        - macOS 15 (Sequoia)
        - macOS 14 (Sonoma)
        - macOS 13 (Ventura)
        - macOS 12 (Monterey)
        - Windows 11
        - Windows 10
        - Linux
    validations:
      required: true

  - type: input
    id: version
    attributes:
      label: Phiên bản Gõ Nhanh
      description: Xem trong menu Gõ Nhanh > About
      placeholder: "1.0.64"

  - type: dropdown
    id: frequency
    attributes:
      label: Tần suất xảy ra
      options:
        - Luôn luôn (100%)
        - Thường xuyên (>50%)
        - Thỉnh thoảng (<50%)
        - Hiếm khi
        - Chỉ một lần
